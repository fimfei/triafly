const CONFIG_API = {
    baseURL: window.location.origin,         // url стенда
    listBlockLength: 1000,                   // длина блока списков для пагинации
    simpleList: false,                       // с сервера будет приходить простой array со строковыми значениями
}

export default CONFIG_API;
