import React from 'react';
import {CoordinatesPanelScaleY} from '.';

export const CoordinatesPanelScaleYRight = props => {
    return (
        <CoordinatesPanelScaleY {...props} linkToScale="right" />
    )
}

CoordinatesPanelScaleYRight.displayName = 'CoordinatesPanelScaleYRight';

export default CoordinatesPanelScaleYRight;
